# Database connection for SchemaSpy
# Copy to .env and fill in your credentials
# .env is gitignored — never committed

DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database
DB_USER=your_user
DB_PASS=your_password

# SchemaSpy version (optional)
SCHEMASPY_VERSION=6.2.4
